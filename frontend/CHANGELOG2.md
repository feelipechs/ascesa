# Changelog 2 — Refatoração Frontend

> Tasks concluídas de 1 a 5, cobrindo qualidade, dados reais, edição inline, animações e dashboard admin.

---

## Task 1 — Convenções e Qualidade

### Server vs Client Component
- `(home)/page.tsx` — removido `'use client'`, extraída lógica interativa para `_sections/home-content.tsx`
- `areas/page.tsx` — removido `'use client'`, extraídos filtros + grid para `_sections/areas-page-content.tsx`

### Exports padronizados (named function)
6 componentes convertidos de `export default const` para `export function`:

| Arquivo | Antes | Depois |
|---|---|---|
| `components/layout/header.tsx` | `export default Header` | `export function Header` |
| `components/layout/footer.tsx` | `export default Footer` | `export function Footer` |
| `(home)/_sections/supporters.tsx` | `export default Supporters` | `export function Supporters` |
| `(home)/_sections/impact-banner.tsx` | `export default ImpactBanner` | `export function ImpactBanner` |
| `(home)/_sections/gallery.tsx` | `export default Gallery` | `export function Gallery` |
| `sobre/_sections/team.tsx` | `export default Team` | `export function Team` |

Importações atualizadas em 7 páginas que consumiam esses componentes.

### Imagens
- `sobre/_sections/team.tsx` — `<img>` substituído por `next/image`

### Paginação compartilhada
- Criado `src/components/shared/pagination.tsx` (`SharedPagination`)
- `projects-pagination.tsx` e `areas-pagination.tsx` agora delegam ao componente compartilhado

### Correções visuais
- Galeria de `/projetos/[slug]`: `grid-cols-2` → `grid-cols-1 sm:grid-cols-2`
- `bg-[#0a1628]` substituído por `bg-foreground` em 7 ocorrências (hero.tsx e header.tsx)

### Loading e error boundaries
Criados 5 novos arquivos:

| Arquivo | Tipo |
|---|---|
| `(home)/loading.tsx` | Skeleton da home |
| `areas/[slug]/loading.tsx` | Skeleton da área |
| `areas/[slug]/error.tsx` | Error boundary |
| `projetos/[slug]/loading.tsx` | Skeleton do projeto |
| `projetos/[slug]/error.tsx` | Error boundary |

### Metadata
- `layout.tsx` — description corrigida de "Generated by create next app" para texto descritivo

### Admin — placeholders em inglês
- `section-cards.tsx` — labels traduzidos para PT-BR
- `site-header.tsx` — título "Dashboard", botão GitHub removido
- `data.json` + `DataTable` + `ChartAreaInteractive` removidos do `admin/page.tsx`

### Renomear isAdmin → isAuthenticated
- `projetos/page.tsx`, `projects-grid.tsx`, `project-card.tsx` — renomeado
- Mantido `isAdmin` em `users/[id]/route.ts` (role check real)

---

## Task 2 — Dados Reais nas Páginas Públicas

### Schema Prisma — extensão do SiteSettings

| Mudança | Detalhe |
|---|---|
| Renomear stats | `statsChildren`/`Years`/`Areas` → `statsOne`/`Two`/`Three` |
| Adicionar | `values`, `socialInstagram`, `socialFacebook`, `socialYoutube`, `socialWhatsapp`, `socialLinkedin` |

`prisma db push --accept-data-loss` + `prisma generate` + seed atualizado.

### Home (`/`)
- `home-content.tsx` busca `useSettings()`, `useGalleryImages({ context: 'HOME' })`, `usePartners()`
- Hero recebe `homeTitle`, `homeSubtitle`, `statsOne/Two/Three` do banco
- Galeria busca imagens com `context: 'HOME'`
- Parceiros vêm da API (fallback para dados hardcoded se vazio)

### Sobre (`/sobre`)
- Criado `sobre-content.tsx` (Client Component) que busca `useSettings()` + `useTeamMembers()`
- `HistorySection` recebe `about` como prop
- `MissionVisionValuesSection` recebe `mission`, `vision`, `values`
- `Team` recebe membros mapeados da API

### Transparência (`/transparencia`)
- Extraídos `DocumentCard` e `DocumentSection` para `_sections/`
- `TransparenciaContent` busca `useDocumentCategories()` e `useDocuments()` com filtros
- Removido `src/lib/transparency-data.ts`
- Corrigido tipo `DocumentListItem` (usava `title` inexistente → `name` + `slug`)

### Contato (`/contato`)
- Criado `SocialLinks` (renderiza condicional por campo preenchido)
- `ContactInfo` aceita props (email, phone, address)
- `ContatoContent` busca `useSettings()`
- SVG icons extraídos do `page.tsx` para o componente `SocialLinks`

### Footer
- Agora busca `SiteSettings` via Prisma server-side (async Server Component)
- Email, telefone, endereço, CNPJ, redes sociais vêm do banco
- Redes sociais renderizadas apenas se preenchidas

### Metadata do layout
- Substituído `export const metadata` estático por `generateMetadata()` async
- Description busca do campo `about` no SiteSettings

---

## Task 3 — Edição Inline nas Páginas Públicas

### 5 novos forms criados

| Form | Localização | Hooks usados |
|---|---|---|
| `testimonial-form.tsx` | `forms/testimonial-form.tsx` | `useTestimonialMutations()` |
| `gallery-image-form.tsx` | `forms/gallery-image-form.tsx` | `useGalleryImageMutations()` |
| `team-member-form.tsx` | `forms/team-member-form.tsx` | `useTeamMemberMutations()` |
| `partner-form.tsx` | `forms/partner-form.tsx` | `usePartnerMutations()` |
| `document-category-form.tsx` | `forms/document-category-form.tsx` | `useDocumentCategoryMutations()` |

Todos seguem o padrão: `'use client'`, hooks de mutação, `AdminSheet`, botão "Salvando... / Salvar alterações / Criar".

### /areas — AreaForm + auth
- `areas/page.tsx` adicionado `auth()`, passa `isAuthenticated`
- `areas-page-content.tsx` gerencia `AdminSheet` com `AreaForm` + Dialog de exclusão
- `area-card.tsx` virou Client Component com `AdminActions`

### /areas/[slug] — TeamMemberForm + auth
- `areas/[slug]/page.tsx` adicionado `auth()`
- Criado `team-section-wrapper.tsx` com `AdminSheet` + `TeamMemberForm`
- `team-section.tsx` virou Client Component com `AdminActions`

### /transparencia — DocumentForm + DocumentCategoryForm + auth
- `transparencia/page.tsx` adicionado `auth()`
- `TransparenciaContent` gerencia `AdminSheet` para DocumentForm e DocumentCategoryForm
- `document-card.tsx` e `document-section.tsx` viraram Client Components com `AdminActions`
- `DocumentForm` ganhou prop `defaultCategoryId` para pre-selecionar categoria

### /projetos/[slug] — TestimonialForm + GalleryImageForm
- `page.tsx` adicionado `auth()`, extraídas seções de depoimentos e galeria
- Criados `testimonials-section.tsx` e `gallery-section.tsx` (Client Components)
- Ambos com `AdminSheet`, formulário e exclusão com confirmação

### Home — PartnerForm + GalleryImageForm
- `(home)/page.tsx` adicionado `auth()`
- `home-content.tsx` gerencia `AdminSheet` para PartnerForm e GalleryImageForm
- `supporters.tsx` — `AdminActions` nos logos + botão "Adicionar"

---

## Task 4 — Animações (Framer Motion)

### Componentes compartilhados criados

| Componente | Localização | Função |
|---|---|---|
| `MotionSection` | `shared/motion-section.tsx` | Fade-in + slide-up ao scroll |
| `MotionGrid` | `shared/motion-grid.tsx` | Stagger container + `item` variant |

### Home
- **Hero**: título e subtítulo com `motion.div` fade-in + slide-up (delays 0.2s e 0.35s)
- **Gallery**: adicionado `'use client'`, envolvido em `MotionSection`
- **Supporters**: logos em `MotionGrid` com stagger

### Áreas
- `areas-grid.tsx`: CSS `animate-in` removido, substituído por `MotionGrid` + stagger
- `areas-page-content.tsx`: grid envolvido em `MotionSection`

### Projetos
- `projects-grid.tsx`: CSS `animate-in` removido, substituído por `MotionGrid` + stagger
- `projects-filters.tsx`: envolvido em `MotionSection`

### Sobre
- `history.tsx`: adicionado `'use client'`, texto em `MotionSection`, timeline em `MotionGrid`
- `mission-vision-values.tsx`: adicionado `'use client'`, seção em `MotionSection`, cards em `MotionGrid`
- `team.tsx`: adicionado `'use client'`, grid em `MotionGrid`

### Transparência
- `transparencia-content.tsx`: cada seção em `MotionSection`
- `document-section.tsx`: grid em `MotionGrid`
- `document-card.tsx`: cada card em `motion.div` com `item` variant

---

## Task 5 — Dashboard Admin

### Limpeza do template shadcn
- Removidos fisicamente: `chart-area-interactive.tsx`, `data-table.tsx` (807 linhas), `data.json`
- `nav-main.tsx` e `nav-user.tsx` — labels traduzidos para PT-BR

### Layout do admin
- Criado `admin/layout.tsx` com `SidebarProvider` + `AppSidebar` + `auth()` guard
- `AppSidebar` — brand "EducaSurf", navegação real (Dashboard, Usuários, Configurações)
- `NavUser` conectado ao `session.user` real
- Removidos componentes mock: `NavDocuments`, seções do `NavSecondary`

### Dashboard (`/admin`)
- `page.tsx` vira Server Component com `prisma.$transaction()` — 8 contagens reais
- `section-cards.tsx` recebe props e renderiza cards com ícones do `@tabler/icons-react`

### Gestão de usuários (`/admin/users`)
- `UserForm` em `forms/user-form.tsx` — create/update com role editável apenas por ADMIN
- Página com `Table` do shadcn, `AdminSheet` + `UserForm`, Dialog de exclusão

### Perfil (`/admin/profile`)
- Informações da conta (name, email — somente leitura)
- Troca de senha via `useUpdateUserPassword()` (já existia)

### Configurações do site (`/admin/settings`)
- `SiteSettingsForm` em `forms/site-settings-form.tsx` — 4 seções separadas por `Separator`:
  1. Informações Gerais (email, phone, address, cnpj)
  2. Conteúdo da Home (homeTitle, homeSubtitle, statsOne/Two/Three)
  3. Sobre a ONG (about, mission, vision, values)
  4. Redes Sociais (socialInstagram/Facebook/Youtube/Whatsapp/Linkedin)
- Usa `useSettings()` para carregar, `useUpdateSettings()` para salvar

---

## Estatísticas

| Métrica | Valor |
|---|---|
| Arquivos criados | ~25 |
| Arquivos modificados | ~45 |
| Forms de CRUD criados | 5 (total agora: 8) |
| Componentes compartilhados criados | 3 (MotionSection, MotionGrid, SharedPagination) |
| Páginas com loading/error boundaries | 3 rotas (5 arquivos) |
| Páginas com Framer Motion | 5 rotas |
| Páginas com edição inline | 5 rotas |
| `npx tsc --noEmit` | Zero erros |
