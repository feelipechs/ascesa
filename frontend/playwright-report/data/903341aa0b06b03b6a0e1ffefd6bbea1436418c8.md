# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: admin/profile.spec.ts >> Admin Perfil >> edita nome do perfil
- Location: e2e/admin/profile.spec.ts:9:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByText('Admin Teste Editado')
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for getByText('Admin Teste Editado')

```

```yaml
- list:
  - listitem:
    - link "Ascesa":
      - /url: /admin
- list:
  - listitem:
    - link "Dashboard":
      - /url: /admin
  - listitem:
    - link "Projetos":
      - /url: /admin/projects
  - listitem:
    - link "Voluntários":
      - /url: /admin/volunteers
  - listitem:
    - link "Usuários":
      - /url: /admin/users
  - listitem:
    - link "Notas Fiscais":
      - /url: /admin/fiscal-notes
  - listitem:
    - link "Configurações":
      - /url: /admin/settings
- list:
  - listitem:
    - button "A Admin teste@teste.com"
- main:
  - button "Toggle Sidebar"
  - heading "Dashboard" [level=1]
  - button "Alternar tema"
  - heading "Meu Perfil" [level=1]
  - paragraph: Gerencie suas informações pessoais e senha.
  - text: Informações da Conta Seu nome e email associados à sua conta. Nome
  - paragraph: Admin
  - text: Email
  - paragraph: teste@teste.com
  - button "Editar"
  - text: Alterar Senha Digite sua senha atual e a nova senha desejada. Senha atual
  - textbox "Senha atual"
  - text: Nova senha
  - textbox "Nova senha"
- region "Notifications alt+T"
- alert
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test'
  2  | 
  3  | test.describe('Admin Perfil', () => {
  4  |   test('carrega página de perfil', async ({ page }) => {
  5  |     await page.goto('/admin/profile')
  6  |     await expect(page.getByText('Meu Perfil')).toBeVisible()
  7  |   })
  8  | 
  9  |   test('edita nome do perfil', async ({ page }) => {
  10 |     // Update name via API directly
  11 |     const resp = await page.request.put('/api/me', { data: { name: 'Admin Teste Editado' } })
  12 |     expect(resp.ok()).toBeTruthy()
  13 | 
  14 |     await page.goto('/admin/profile')
> 15 |     await expect(page.getByText('Admin Teste Editado')).toBeVisible({ timeout: 15000 })
     |                                                         ^ Error: expect(locator).toBeVisible() failed
  16 |   })
  17 | 
  18 |   test('altera senha com sucesso', async ({ page }) => {
  19 |     // Change password via API directly
  20 |     const resp = await page.request.put('/api/me', {
  21 |       data: { currentPassword: '123', password: '123456' },
  22 |     })
  23 |     expect(resp.ok()).toBeTruthy()
  24 | 
  25 |     await page.goto('/admin/profile')
  26 |     await expect(page.getByText('Perfil atualizado!').or(page.getByText('atualizado')).first()).toBeVisible({ timeout: 15000 })
  27 |   })
  28 | 
  29 |   test('cancela edição do perfil', async ({ page }) => {
  30 |     await page.goto('/admin/profile')
  31 |     await page.getByRole('button', { name: /editar/i }).first().click()
  32 | 
  33 |     const nomeInput = page.getByLabel(/nome/i)
  34 |     await nomeInput.clear()
  35 |     await nomeInput.fill('Nome Descartado')
  36 | 
  37 |     await page.getByRole('button', { name: /cancelar/i }).click()
  38 | 
  39 |     await expect(page.getByText('Meu Perfil')).toBeVisible()
  40 |   })
  41 | })
  42 | 
```