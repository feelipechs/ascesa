# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: admin/volunteers.spec.ts >> Admin Voluntários >> edita voluntário existente via API
- Location: e2e/admin/volunteers.spec.ts:57:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByText('Ana Lima Editada').first()
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for getByText('Ana Lima Editada').first()

```

```yaml
- list:
  - listitem:
    - link "Ascesa":
      - /url: /admin
- list:
  - listitem:
    - link "Dashboard":
      - /url: /admin
  - listitem:
    - link "Projetos":
      - /url: /admin/projects
  - listitem:
    - link "Voluntários":
      - /url: /admin/volunteers
  - listitem:
    - link "Usuários":
      - /url: /admin/users
  - listitem:
    - link "Notas Fiscais":
      - /url: /admin/fiscal-notes
  - listitem:
    - link "Configurações":
      - /url: /admin/settings
- list:
  - listitem:
    - button "A Admin teste@teste.com"
- main:
  - button "Toggle Sidebar"
  - heading "Dashboard" [level=1]
  - button "Alternar tema"
  - heading "Voluntários" [level=1]
  - button "Novo Voluntário"
  - textbox "Pesquisar..."
  - button "Colunas"
  - button "Exportar"
  - table:
    - rowgroup:
      - row "Nome Email Telefone Cadastro Ações":
        - columnheader "Nome"
        - columnheader "Email"
        - columnheader "Telefone"
        - columnheader "Cadastro"
        - columnheader "Ações"
    - rowgroup:
      - row "Ana Lima ana.lima@email.com (11) 98888-1111 31/05/2026":
        - cell "Ana Lima"
        - cell "ana.lima@email.com"
        - cell "(11) 98888-1111"
        - cell "31/05/2026"
        - cell:
          - button
          - button
  - text: 0 de 1 linha(s) selecionada(s). Linhas por página
  - combobox: "10"
  - text: Página 1 de 1
  - button "Primeira página" [disabled]
  - button "Página anterior" [disabled]
  - button "Próxima página" [disabled]
  - button "Última página" [disabled]
- region "Notifications alt+T"
- alert
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test'
  2  | 
  3  | test.describe('Admin Voluntários', () => {
  4  |   test('carrega página de voluntários', async ({ page }) => {
  5  |     await page.goto('/admin/volunteers')
  6  |     await expect(page.getByText(/voluntários/i).first()).toBeVisible()
  7  |   })
  8  | 
  9  |   test('exibe voluntário do seed na tabela', async ({ page }) => {
  10 |     await page.goto('/admin/volunteers')
  11 |     await expect(page.getByText('Ana Lima').first()).toBeVisible({ timeout: 15000 })
  12 |   })
  13 | 
  14 |   test('abre formulário de novo voluntário', async ({ page }) => {
  15 |     await page.goto('/admin/volunteers')
  16 |     await page.getByRole('button', { name: /novo voluntário/i }).click()
  17 |     await expect(page.getByRole('heading', { name: 'Novo voluntário' })).toBeVisible()
  18 |     await expect(page.getByLabel(/nome/i)).toBeVisible()
  19 |     await expect(page.getByLabel(/email/i)).toBeVisible()
  20 |   })
  21 | 
  22 |   test('preenche formulário de voluntário e submete', async ({ page }) => {
  23 |     await page.goto('/admin/volunteers')
  24 |     await page.getByRole('button', { name: /novo voluntário/i }).click()
  25 | 
  26 |     await page.getByLabel(/nome/i).fill('Carlos Teste')
  27 |     await page.getByLabel(/email/i).fill('carlos.teste3@example.com')
  28 |     await page.getByLabel(/telefone/i).fill('(11) 97777-6666')
  29 | 
  30 |     await page.getByRole('button', { name: /adicionar voluntário/i }).click()
  31 | 
  32 |     await expect(page.getByText('Voluntário criado').or(page.getByText('voluntário')).first()).toBeVisible({ timeout: 15000 })
  33 |   })
  34 | 
  35 |   test('campo de busca filtra voluntários', async ({ page }) => {
  36 |     await page.goto('/admin/volunteers')
  37 |     const searchInput = page.getByPlaceholder(/pesquisar/i)
  38 |     if (await searchInput.isVisible({ timeout: 10000 })) {
  39 |       await searchInput.fill('Ana')
  40 |       await page.waitForTimeout(500)
  41 |       await expect(page.getByText('Ana Lima').first()).toBeVisible()
  42 |     }
  43 |   })
  44 | 
  45 |   test('valida campos obrigatórios no formulário', async ({ page }) => {
  46 |     await page.goto('/admin/volunteers')
  47 |     await page.getByRole('button', { name: /novo voluntário/i }).click()
  48 |     await expect(page.getByRole('heading', { name: 'Novo voluntário' })).toBeVisible()
  49 | 
  50 |     await page.evaluate(() => document.querySelector('form')?.setAttribute('novalidate', ''))
  51 |     await page.getByRole('button', { name: /adicionar voluntário/i }).click()
  52 | 
  53 |     await expect(page.getByText('Nome obrigatório')).toBeVisible({ timeout: 5000 })
  54 |     await expect(page.getByText('Email inválido')).toBeVisible()
  55 |   })
  56 | 
  57 |   test('edita voluntário existente via API', async ({ page }) => {
  58 |     // Get current volunteers to find the seed volunteer's ID
  59 |     await page.goto('/admin/volunteers')
  60 |     await expect(page.getByText('Ana Lima').first()).toBeVisible({ timeout: 15000 })
  61 | 
  62 |     // Parse the table to get the first volunteer's ID from the edit button URL
  63 |     const editButton = page.locator('tbody tr').first().locator('td:last-child button:first-child')
  64 |     const onclickAttr = await editButton.getAttribute('onclick') || ''
  65 | 
  66 |     // No onclick — use the row click handler
  67 |     await editButton.click()
  68 |     await expect(page.getByRole('heading', { name: 'Editar voluntário' })).toBeVisible()
  69 | 
  70 |     await page.getByLabel(/nome/i).clear()
  71 |     await page.getByLabel(/nome/i).fill('Ana Lima Editada')
  72 | 
  73 |     // Submit via JavaScript
  74 |     await page.evaluate(() => {
  75 |       const btn = document.querySelector('button[type="submit"]')
  76 |       if (btn instanceof HTMLElement) btn.click()
  77 |     })
  78 |     await page.waitForTimeout(2000)
  79 | 
  80 |     await page.goto('/admin/volunteers')
> 81 |     await expect(page.getByText('Ana Lima Editada').first()).toBeVisible({ timeout: 15000 })
     |                                                              ^ Error: expect(locator).toBeVisible() failed
  82 |   })
  83 | 
  84 |   test('deleta voluntário via DeleteDialog', async ({ page }) => {
  85 |     await page.goto('/admin/volunteers')
  86 |     const deleteButton = page.locator('tbody tr').first().locator('td:last-child button.text-destructive')
  87 |     if (await deleteButton.isVisible({ timeout: 5000 })) {
  88 |       await deleteButton.click()
  89 |       await expect(page.getByText(/tem certeza/i)).toBeVisible()
  90 |       await page.getByRole('button', { name: /confirmar|excluir/i }).last().click()
  91 |       await expect(page.getByText(/tem certeza/i)).not.toBeVisible({ timeout: 10000 })
  92 |     }
  93 |   })
  94 | })
  95 | 
```